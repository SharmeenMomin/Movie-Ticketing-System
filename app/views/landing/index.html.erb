<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome - Movie Ticketing System</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f9;
      margin: 0;
      padding: 0;
      text-align: center;
    }
    h1 {
      color: #2c3e50;
      font-size: 2.5em;
      margin-top: 50px;
    }
    p {
      font-size: 1.2em;
      color: #34495e;
    }
    ul {
      list-style-type: none;
      padding: 0;
      margin-top: 30px;
    }
    li {
      margin-bottom: 15px;
    }
    a {
      text-decoration: none;
      padding: 12px 20px;
      background-color: #3498db;
      color: white;
      font-size: 1.2em;
      border-radius: 5px;
      transition: background-color 0.3s;
    }
    a:hover {
      background-color: #2980b9;
    }
  </style>
</head>
<body>

  <h1>Welcome to the Movie Ticketing System</h1>

  <p>Choose your portal:</p>

  <ul>
    <li><a href="<%= home_path %>">User Portal</a></li><br>
    <li>
      <% if admin_signed_in? %>
        <%= link_to "Admin Panel", admin_dashboard_path, class: 'admin-link' %> <!-- Redirects to admin dashboard if logged in -->
      <% else %>
        <%= link_to "Admin Panel", home_path, class: 'admin-link' %> <!-- Redirects to login page if not logged in -->
      <% end %>
    </li>
  </ul>

</body>
</html>
