<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Movie Ticketing System</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f9;
      margin: 0;
      padding: 0;
    }
    #notice {
      text-align: center;
      color: #e74c3c;
      font-size: 1.2em;
    }
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 20px;
    }
    .user-info {
      text-align: center;
      font-size: 1.2em;
      margin-top: 20px;
    }
    header {
      background-color: #2c3e50;
      color: #ecf0f1;
      padding: 20px;
      text-align: center;
    }
    header h1 {
      margin: 0;
      font-size: 3em;
    }
    .links {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 20px;
      margin-top: 30px;
    }
    .links a {
      padding: 12px 25px;
      background-color: #3498db;
      color: white;
      text-decoration: none;
      border-radius: 5px;
      font-size: 1.1em;
    }
    .links a:hover {
      background-color: #2980b9;
    }
    .auth-links {
      margin-top: 30px;
      text-align: center;
      font-size: 1.2em;
    }
    .auth-links a {
      background-color: #3498db;
      color: white;
      text-decoration: none;
    }
    .auth-links a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>

  <div id="notice"><%= notice %></div>
  <div class="container">
    <header>
      <h1>Movie Ticketing System</h1>
    </header>

    <div class="links">
      <% if current_user %>
        <% if current_user.username == "Admin" %>
          <%= link_to "Go to Admin Dashboard", admin_dashboard_path %>
        <% else%>
          <div class="user-info">
            Logged in as <%= current_user.name %>
          </div>
          <%= link_to 'View All Movies', movies_path %>
          <%= link_to 'View Profile', user_path(current_user.id) %>
          <%= link_to 'View My Booking History', tickets_user_path(current_user.id) %>
          <%= link_to 'Logout', user_logout_path %>
        <%end %>
      <% else %>
        <div class="auth-links">
          <%= link_to 'Signup', user_signup_path %> or
          <%= link_to 'Login', user_login_path %>
        </div>
      <% end %>
    </div>
  </div>

</body>
</html>
