<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>New User - Movie Ticketing System</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f9;
      margin: 0;
      padding: 0;
    }
    #notice {
      text-align: center;
      color: #e74c3c;
      font-size: 1.2em;
    }
    .container {
      max-width: 1500px;
      margin: 0 auto;
      padding: 20px;
    }
    .form-container {
      background-color: #fff;
      padding: 40px;
      border-radius: 12px;
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 500px;
      text-align: center;
      margin: 0 auto;
    }
    .form-container h1 {
      color: #2c3e50;
      font-size: 2.5em;
      margin-bottom: 25px;
      font-weight: 600;
    }
    .form-group {
      margin-bottom: 25px;
    }
    label {
      display: block;
      font-size: 1.2em;
      color: #34495e;
      margin-top: 4px;
      margin-bottom: 4px;
      text-align: left;
    }
    input {
      width: 100%;
      padding: 7px;
      font-size: 1.1em;
      border: 1px solid #ccc;
      border-radius: 8px;
      box-sizing: border-box;
      transition: border-color 0.3s;
    }
    input:focus {
      border-color: #3498db;
      outline: none;
    }
    input[type="submit"] {
      background-color: #3498db;
      color: white;
      font-weight: bold;
      border: none;
      cursor: pointer;
      padding: 15px;
      font-size: 1.2em;
      border-radius: 8px;
      margin-top: 10px;
      width: 70%;
      transition: background-color 0.3s;
    }
    input[type="submit"]:hover {
      background-color: #2980b9;
    }
    .auth-links {
      margin-top: 15px;
      font-size: 1.1em;
    }
    .auth-links a {
      color: #3498db;
      text-decoration: none;
    }
    .auth-links a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>

  <div id="notice"><%= notice %></div>
  <div class="container">
    <div class="form-container">
      <h1>New User</h1>

      <%= render "form", user: @user %>

      <% if current_user&.username != "Admin" %>
        <div class="auth-links">
          <%= link_to "Login Instead", home_index_path %>
        </div>
      <% end %>
    </div>
  </div>

</body>
</html>
